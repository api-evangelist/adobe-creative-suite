naftiko: 1.0.0-alpha2
info:
  label: Adobe Creative Suite Adobe PDF Services API — Combine PDF
  description: 'Adobe Creative Suite Adobe PDF Services API — Combine PDF. 1 operations. Lead operation: Adobe Creative Suite
    Combine PDFs. Self-contained Naftiko capability covering one Adobe Creative Suite business surface.'
  tags:
  - Adobe Creative Suite
  - Combine PDF
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_CREATIVE_SUITE_API_KEY: ADOBE_CREATIVE_SUITE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-combine-pdf
    baseUri: https://pdf-services.adobe.io
    description: Adobe Creative Suite Adobe PDF Services API — Combine PDF business capability. Self-contained, no shared
      references.
    resources:
    - name: operation-combinepdf
      path: /operation/combinepdf
      operations:
      - name: combinepdfs
        method: POST
        description: Adobe Creative Suite Combine PDFs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_CREATIVE_SUITE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-combine-pdf-rest
    port: 8080
    description: REST adapter for Adobe Creative Suite Adobe PDF Services API — Combine PDF. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/operation/combinepdf
      name: operation-combinepdf
      description: REST surface for operation-combinepdf.
      operations:
      - method: POST
        name: combinepdfs
        description: Adobe Creative Suite Combine PDFs
        call: pdf-services-combine-pdf.combinepdfs
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-combine-pdf-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe Creative Suite Adobe PDF Services API — Combine PDF. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: adobe-creative-suite-combine-pdfs
      description: Adobe Creative Suite Combine PDFs
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-combine-pdf.combinepdfs
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
